import React from "react"

export default class HeadingFiveComponent extends React.Component {
    render() {
        return (
            <>
                <h5>{this.props.title}</h5>
            </>
        )
    }
}